# Preview render notes

The local HTML/CSS preview successfully rendered at `http://127.0.0.1:4173/preview.html`.

The rendered viewport shows a DeepSeek Harness-style two-column layout. The left sidebar contains the DeepSeek/HARNESS brand, New Chat, workspace and conversation list, a real-looking `API 余额` card above Settings, and a CNY balance of `¥6.66` against a daily baseline of `¥10.00` with a 66.6% progress bar. The main panel contains a conversation view and composer so the balance card is shown in its intended product context rather than as a standalone sketch.

The browser-generated screenshot is saved at `/home/ubuntu/screenshots/127_0_0_1_2026-08-18_08-28-27_6038.webp` and should be copied into this repository as `assets/dsh-money-view-preview.webp`.
